# Description

This script is used to get the block timestamp for a given block number.

## Usage

```bash
BLOCK_NUMBER=1000 yarn contracts:get-block-timestamp:local
```

## Environment Variables

- `BLOCK_NUMBER`: Optional block number to get the timestamp for. If not provided, the current block number will be used.

## Output

The output will be the block timestamp for the given block number.
